
version: 2

sources:
  - name: raw
    database: "{{ env_var('DB_NAME', 'medical_warehouse') }}"
    schema: raw
    tables:
      - name: telegram_messages
        description: Raw telegram messages data
      - name: image_detections
        description: Raw image detection results
